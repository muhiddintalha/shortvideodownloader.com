# Copy this file to .env and fill in values before deploying.
# .env is gitignored — never commit it.

# Temporary directory for video downloads (already set in docker-compose)
# TMPDIR=/tmp/svd-downloads

# Optional: set to "production" to suppress debug output
# APP_ENV=production
